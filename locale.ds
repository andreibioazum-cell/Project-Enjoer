include graphics

function tr_play()
    if language == 1
        return "Играть"
    end
    return "Play"
end

function tr_settings()
    if language == 1
        return "Настройки"
    end
    return "Settings"
end

function tr_solo()
    if language == 1
        return "Соло"
    end
    return "Solo"
end

function tr_online()
    if language == 1
        return "Онлайн"
    end
    return "Online"
end

function tr_console()
    if language == 1
        return "Консоль"
    end
    return "Console"
end

function tr_up()
    if language == 1
        return "Вверх"
    end
    return "Up"
end

function tr_down()
    if language == 1
        return "Вниз"
    end
    return "Down"
end

function tr_clear()
    if language == 1
        return "Очистить"
    end
    return "Clear"
end

function tr_back()
    if language == 1
        return "Назад"
    end
    return "Back"
end

function tr_punch()
    if language == 1
        return "Удар"
    end
    return "Punch"
end

function tr_tap_to_return()
    if language == 1
        return "Нажмите, чтобы выйти"
    end
    return "Tap to return"
end

function tr_victory()
    if language == 1
        return "Победа!"
    end
    return "Victory!"
end

function tr_opponent_defeated()
    if language == 1
        return "Соперник побеждён"
    end
    return "Opponent defeated"
end

function tr_defeat()
    if language == 1
        return "Поражение"
    end
    return "Defeat"
end

function tr_opponent_won()
    if language == 1
        return "Соперник победил"
    end
    return "The opponent won"
end

function tr_you_were_defeated()
    if language == 1
        return "Вы повержены"
    end
    return "You were defeated"
end

function tr_no_connection()
    if language == 1
        return "Нет соединения"
    end
    return "No connection"
end

function tr_reconnecting()
    if language == 1
        return "Переподключение..."
    end
    return "Reconnecting..."
end

function tr_connecting()
    if language == 1
        return "Подключение..."
    end
    return "Connecting..."
end

function tr_chat()
    if language == 1
        return "Чат"
    end
    return "Chat"
end

function tr_no_messages()
    if language == 1
        return "Нет сообщений"
    end
    return "No messages"
end

function tr_close()
    if language == 1
        return "Закрыть"
    end
    return "Close"
end

function tr_type_message()
    if language == 1
        return "Введите сообщение..."
    end
    return "Type a message..."
end

function tr_send()
    if language == 1
        return "Отправить"
    end
    return "Send"
end

function tr_account()
    if language == 1
        return "Аккаунт"
    end
    return "Account"
end

function tr_achievements()
    if language == 1
        return "Достижения"
    end
    return "Achievements"
end

function tr_achievements_title()
    if language == 1
        return "Достижения"
    end
    return "Achievements"
end

// Все названия и описания достижений переведены на оба языка: RU и EN.
function tr_achievement_welcome()
    if language == 1
        return "Привет!"
    end
    return "Hello!"
end

function tr_achievement_welcome_desc()
    if language == 1
        return "Зайти в игру впервые"
    end
    return "Enter the game for the first time"
end

function tr_achievement_grass()
    if language == 1
        return "Потрогай траву"
    end
    return "Touch grass"
end

function tr_achievement_grass_desc()
    if language == 1
        return "Собери всех персонажей в игре"
    end
    return "Collect all the characters in the game"
end

function tr_achievement_open()
    if language == 1
        return "Получено"
    end
    return "Unlocked"
end

function tr_achievement_locked()
    if language == 1
        return "Не получено"
    end
    return "Locked"
end

function tr_achievement_toast_title()
    if language == 1
        return "ДОСТИЖЕНИЕ"
    end
    return "ACHIEVEMENT"
end

function tr_achievement_first_win()
    if language == 1
        return "Первая победа"
    end
    return "First win"
end

function tr_achievement_first_win_desc()
    if language == 1
        return "Выиграть первый бой"
    end
    return "Win your first battle"
end

function tr_achievement_first_buy()
    if language == 1
        return "Покупка"
    end
    return "First purchase"
end

function tr_achievement_first_buy_desc()
    if language == 1
        return "Купить первого персонажа"
    end
    return "Buy your first character"
end

function tr_login_title()
    if language == 1
        return "Вход и регистрация"
    end
    return "Login / Register"
end

function tr_nick()
    if language == 1
        return "Ник"
    end
    return "Nick"
end

function tr_pass()
    if language == 1
        return "Пароль"
    end
    return "Password"
end

function tr_nick_ph()
    if language == 1
        return "Ник: 1-16 (буквы, цифры, _)"
    end
    return "Nick: 1-16 (letters, digits, _)"
end

function tr_pass_ph()
    if language == 1
        return "Пароль: от 1 символа"
    end
    return "Password: 1+ chars"
end

function tr_bad_nick()
    if language == 1
        return "Ник: от 1 до 16 символов, только буквы, цифры и _"
    end
    return "Nick: 1-16 chars, letters, digits and _ only"
end

function tr_wrong_pass()
    if language == 1
        return "Неверный пароль для этого ника"
    end
    return "Wrong password for this nick"
end

function tr_bad_pass()
    if language == 1
        return "Пароль: от 1 до 32 символов"
    end
    return "Password: 1-32 characters"
end

function tr_login_btn()
    if language == 1
        return "Войти / Зарегистрироваться"
    end
    return "Log In / Register"
end

function tr_your_nick()
    if language == 1
        return "Ваш ник: "
    end
    return "Your nick: "
end

function tr_leaderboard()
    if language == 1
        return "Лидерборд"
    end
    return "Leaderboard"
end

function tr_leaderboard_title()
    if language == 1
        return "Лидерборд (кубки)"
    end
    return "Leaderboard (cups)"
end

function tr_loading()
    if language == 1
        return "Загрузка..."
    end
    return "Loading..."
end

function tr_no_data()
    if language == 1
        return "Нет данных"
    end
    return "No data"
end

// Предупреждение об эпилепсии — оба языка сразу, язык настроек не важен.
function tr_warn_title_en()
    return "WARNING"
end

function tr_warn_title_ru()
    return "ВНИМАНИЕ"
end

function tr_warn_en1()
    return "This game contains bright flashes and"
end

function tr_warn_en2()
    return "fast color changes (disco mode online)."
end

function tr_warn_en3()
    return "If you have epilepsy or are sensitive"
end

function tr_warn_en4()
    return "to light, please play with caution."
end

function tr_warn_ru1()
    return "В игре есть яркие вспышки и быстрая"
end

function tr_warn_ru2()
    return "смена цветов (диско-режим в онлайне)."
end

function tr_warn_ru3()
    return "При эпилепсии или чувствительности"
end

function tr_warn_ru4()
    return "к свету играйте осторожно."
end

function tr_hp()
    if language == 1
        return "Здоровье"
    end
    return "HP"
end

function tr_strength()
    if language == 1
        return "Сила"
    end
    return "Power"
end

function tr_shop()
    if language == 1
        return "Магазин"
    end
    return "Shop"
end

function tr_box()
    if language == 1
        return "Ящик"
    end
    return "Box"
end

function tr_candies()
    if language == 1
        return "Леденцы"
    end
    return "Candies"
end

function tr_got()
    if language == 1
        return "Выпало:"
    end
    return "You got:"
end

function tr_class_ordinary()
    if language == 1
        return "Обычный"
    end
    return "Ordinary"
end

function tr_class_azum()
    if language == 1
        return "Азум"
    end
    return "Azum"
end

function tr_class_ordinary_desc()
    return "I was the first"
end

function tr_class_azum_desc()
    return "NEVER GIVE UP!"
end

function tr_class_santa()
    if language == 1
        return "Дед Мороз"
    end
    return "Santa"
end

function tr_class_santa_desc()
    return "It's 12:00 AM!"
end

function tr_class_ebuc()
    if language == 1
        return "буК"
    end
    return "ebuC"
end

// Описание бука не переводится — как и девизы остальных классов.
function tr_class_ebuc_desc()
    return "The universe's chaos!"
end

function tr_super()
    if language == 1
        return "Снежинка"
    end
    return "Snowflake"
end

function tr_super_ebuc()
    if language == 1
        return "Ульта"
    end
    return "Ult"
end

function tr_class_selected()
    if language == 1
        return "Выбран"
    end
    return "Selected"
end

function tr_class_pick()
    if language == 1
        return "Выбрать"
    end
    return "Select"
end

function tr_your_class()
    if language == 1
        return "Класс: "
    end
    return "Class: "
end

function tr_levels()
    if language == 1
        return "Уровни"
    end
    return "Levels"
end

function tr_level()
    if language == 1
        return "Уровень"
    end
    return "Level"
end

function tr_level_absorb()
    if language == 1
        return "Поглощение урона"
    end
    return "Damage absorb"
end

function tr_levels_for_class()
    if language == 1
        return "Уровни класса"
    end
    return "Class levels"
end

function tr_class_level_effect(number cls, number n)
    if cls == 1 && n == 1
        if language == 1
            return "Вторая жизнь: урон -10%"
        end
        return "Second life: damage -10%"
    end
    if cls == 1 && n == 2
        if language == 1
            return "Скорость +3%"
        end
        return "Speed +3%"
    end
    if cls == 1 && n == 3
        if language == 1
            return "Атака лечит +0.1 HP"
        end
        return "Attack heals +0.1 HP"
    end
    if cls == 2 && n == 1
        if language == 1
            return "Тик яда +0.15 урона"
        end
        return "Poison tick +0.15 damage"
    end
    if cls == 2 && n == 2
        if language == 1
            return "Тик яда +0.15 урона (x2)"
        end
        return "Poison tick +0.15 damage (x2)"
    end
    if cls == 2 && n == 3
        if language == 1
            return "Яд стакается, снежинка наносит урон"
        end
        return "Poison stacks, snowflake deals damage"
    end
    if cls == 3 && n == 1
        if language == 1
            return "Турель +2 HP"
        end
        return "Turret +2 HP"
    end
    if cls == 3 && n == 2
        if language == 1
            return "Спавн турели лечит +0.5 HP"
        end
        return "Placing a turret heals +0.5 HP"
    end
    if cls == 3 && n == 3
        if language == 1
            return "Луч турели лечит +0.1 HP/сек"
        end
        return "Turret beam heals +0.1 HP/sec"
    end
    if cls == 0 && n == 1
        if language == 1
            return "Урон атаки +5%"
        end
        return "Attack damage +5%"
    end
    if cls == 0 && n == 2
        if language == 1
            return "Скорость +2%"
        end
        return "Speed +2%"
    end
    if language == 1
        return "Атака лечит +0.05 HP"
    end
    return "Attack heals +0.05 HP"
end

function tr_level_unlock()
    if language == 1
        return "Открыть за"
    end
    return "Unlock for"
end

function tr_cups_unit()
    if language == 1
        return "кубков"
    end
    return "cups"
end

function tr_level_locked()
    if language == 1
        return "Сначала откройте предыдущий"
    end
    return "Unlock the previous level first"
end

function tr_level_need_cups()
    if language == 1
        return "Не хватает кубков для уровня"
    end
    return "Not enough cups for this level"
end

function tr_levels_soon()
    if language == 1
        return "Скоро!"
    end
    return "Coming soon!"
end

function tr_cups()
    if language == 1
        return "Кубки"
    end
    return "Cups"
end

function tr_buy_for(number cost, string currency)
    string out = ""
    if language == 1
        out = "Купить · " + cost + " " + currency
    else
        out = "Buy · " + cost + " " + currency
    end
    return out
end

function tr_class_need_cups()
    if language == 1
        return "Не хватает кубков"
    end
    return "Not enough cups"
end

// ── Хэллоуинский батл пасс ────────────────────────────────────────────────
function tr_battlepass()
    if language == 1
        return "Батл пасс"
    end
    return "Battle Pass"
end

function tr_battlepass_title()
    if language == 1
        return "Хэллоуинский батл пасс"
    end
    return "Halloween Battle Pass"
end

function tr_bp_level()
    if language == 1
        return "Уровень"
    end
    return "Level"
end

function tr_bp_bought()
    if language == 1
        return "Куплено"
    end
    return "Bought"
end

function tr_bp_buy()
    if language == 1
        return "Купить"
    end
    return "Buy"
end

function tr_bp_locked()
    if language == 1
        return "Заблокировано"
    end
    return "Locked"
end

function tr_bp_need_prev()
    if language == 1
        return "Сначала купи предыдущий уровень"
    end
    return "Buy the previous level first"
end

function tr_bp_need_cups()
    if language == 1
        return "Не хватает кубков для уровня"
    end
    return "Not enough cups for this level"
end

function tr_bp_reward_1()
    if language == 1
        return "Обычный класс: сразу 3 уровень"
    end
    return "Ordinary class: instant level 3"
end

function tr_bp_reward_2()
    if language == 1
        return "Скин «Зомби» на Азума"
    end
    return "Azum \"Zombie\" skin"
end

// ── Скины Азума ───────────────────────────────────────────────────────────
function tr_skins()
    if language == 1
        return "Скины"
    end
    return "Skins"
end

function tr_skin_normal()
    if language == 1
        return "Обычный"
    end
    return "Normal"
end

function tr_skin_zombie()
    if language == 1
        return "Зомби"
    end
    return "Zombie"
end

function tr_skin_zombie_locked()
    if language == 1
        return "Открой 2 уровень батл пасса"
    end
    return "Unlock battle pass level 2"
end

function tr_cups_reward(number amount)
    if language == 1
        return "+" + amount + " кубков"
    end
    return "+" + amount + " Cups"
end

function tr_banned()
    if language == 1
        return "Вы забанены"
    end
    return "You are banned"
end

function tr_admin_event_on()
    if language == 1
        return "ДИСКО ИВЕНТ ВКЛЮЧЕН!"
    end
    return "DISCO EVENT STARTED!"
end

function tr_admin_event_off()
    if language == 1
        return "Ивент выключен"
    end
    return "Event ended"
end
