/* Generated by DimScript (tools/aot.py). Do not edit by hand. */
/* Source: examples/clicker.ds */
#include "dimscript_runtime.h"
#include <string.h>

typedef struct ClickerGame {
    int32_t score;
    float text_scale;
} ClickerGame;

/* ClickerGame: only plain values, no dtor. */

static ClickerGame * g_game = NULL;

static int ds_program_initialized;

/* 3 script literal(s), interned once at load time. */
static const DsLiteralSource ds_literal_sources[] = {
    {"Главное меню", 23},
    {"Счет: ", 10},
    {"Тапни по экрану, чтобы заработать", 61},
};
static DsString **ds_literals;
static void ds_fn_load(void);
static void ds_fn_touchpressed(int32_t id, float touch_x, float touch_y);
static void ds_fn_update(float dt);
static void ds_fn_draw(void);
static void ds_fn_quit(void);

static void ds_fn_load(void) {
    ((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:13"))->score = 0;
    ((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:14"))->text_scale = 1.0f;
    return;
}

static void ds_fn_touchpressed(int32_t id, float touch_x, float touch_y) {
    (void)id;
    (void)touch_x;
    (void)touch_y;
    ((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:18"))->score = (((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:18"))->score + 1);
    ((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:19"))->text_scale = 1.6f;
    return;
}

static void ds_fn_update(float dt) {
    (void)dt;
    if (((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:24"))->text_scale > 1.0f) {
        ((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:25"))->text_scale = (((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:25"))->text_scale - (4.0f * dt));
        if (((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:26"))->text_scale < 1.0f) {
            ((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:27"))->text_scale = 1.0f;
        }
    }
    return;
}

static void ds_fn_draw(void) {
    ds_render_color((float)((float)(1.0f)), (float)((float)(1.0f)), (float)((float)(1.0f)));
    ds_render_text(ds_literals[0], (float)(20.0f), (float)(20.0f), (float)(1.0f));
    ds_render_text(ds_concat(ds_literals[1], ds_int_to_string((int64_t)(((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:39"))->score))), (float)(50.0f), (float)(150.0f), (float)(((ClickerGame *)ds_require((void *)(g_game), "examples/clicker.ds:39"))->text_scale));
    ds_render_text(ds_literals[2], (float)(50.0f), (float)(300.0f), (float)(0.8f));
    return;
}

static void ds_fn_quit(void) {
    ds_release_slot((void **)&g_game);
    return;
}

void dimscript_init(void) {
    if (ds_program_initialized) return;
    ds_literals = ds_intern_literals(ds_literal_sources, 3);
    g_game = (ClickerGame *)ds_alloc(sizeof(ClickerGame), NULL);
    ds_program_initialized = 1;
}

void dimscript_shutdown(void) {
    if (!ds_program_initialized) return;
    if (g_game) ds_release((void*)g_game);
    g_game = NULL;
    ds_program_initialized = 0;
}

void dimscript_load(void) {
    if (!ds_program_initialized) return;
    ds_fn_load();
}
void dimscript_resized(float width, float height) {
    (void)width;
    (void)height;
}
void dimscript_touchpressed(int id, float touch_x, float touch_y) {
    if (!ds_program_initialized) return;
    ds_fn_touchpressed(id, touch_x, touch_y);
}
void dimscript_touchmoved(int id, float touch_x, float touch_y) {
    (void)id;
    (void)touch_x;
    (void)touch_y;
}
void dimscript_touchreleased(int id, float touch_x, float touch_y) {
    (void)id;
    (void)touch_x;
    (void)touch_y;
}
void dimscript_keypressed(const char *name) {
    (void)name;
}
void dimscript_keyreleased(const char *name) {
    (void)name;
}
void dimscript_update(float dt) {
    if (!ds_program_initialized) return;
    ds_fn_update(dt);
}
void dimscript_draw(void) {
    if (!ds_program_initialized) return;
    ds_fn_draw();
}
void dimscript_quit(void) {
    if (!ds_program_initialized) return;
    ds_fn_quit();
}
