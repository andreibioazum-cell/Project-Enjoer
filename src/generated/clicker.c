/* Generated by DimScript. Do not edit by hand. */
/* Source: examples/clicker.ds */
#include "dimscript_runtime.h"
#include <stdint.h>
#include <stddef.h>
#include <string.h>

typedef struct ClickerGame {
    int32_t score;
    float text_scale;
} ClickerGame;

static ClickerGame * game = NULL;

static int ds_program_initialized;

void dimscript_init(void) {
    if (ds_program_initialized) return;
    game = (ClickerGame *)ds_alloc(sizeof(ClickerGame));
    ds_program_initialized = 1;
}

void dimscript_shutdown(void) {
    if (!ds_program_initialized) return;
    ds_delete((void **)&game);
    ds_program_initialized = 0;
}

static void ds_fn_load(void) {
    game->score = 0;
    game->text_scale = 1.0f;
    return;
}

static void ds_fn_touchpressed(int32_t id, float touch_x, float touch_y) {
    (void)id;
    (void)touch_x;
    (void)touch_y;
    game->score = (game->score + 1);
    game->text_scale = 1.6f;
    return;
}

static void ds_fn_update(float dt) {
    (void)dt;
    if ((game->text_scale > 1.0f)) {
        game->text_scale = (game->text_scale - (4.0f * dt));
        if ((game->text_scale < 1.0f)) {
            game->text_scale = 1.0f;
        }
    }
    return;
}

static void ds_fn_draw(void) {
    ds_render_color((float)(1.0f), (float)(1.0f), (float)(1.0f));
    ds_render_text("Главное меню", (float)(20.0f), (float)(20.0f), (float)(1.0f));
    ds_render_text(ds_concat("Счет: ", ds_int_to_string((int64_t)(game->score))), (float)(50.0f), (float)(150.0f), (float)(game->text_scale));
    ds_render_text("Тапни по экрану, чтобы заработать", (float)(50.0f), (float)(300.0f), (float)(0.8f));
    return;
}

static void ds_fn_quit(void) {
    ds_delete((void **)&game);
    return;
}

void dimscript_load(void) {
    ds_fn_load();
}
void dimscript_touchpressed(int id, float touch_x, float touch_y) {
    ds_fn_touchpressed(id, touch_x, touch_y);
}
void dimscript_update(float dt) {
    ds_fn_update(dt);
}
void dimscript_draw(void) {
    ds_fn_draw();
}
void dimscript_quit(void) {
    ds_fn_quit();
}
