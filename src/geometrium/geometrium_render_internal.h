/* The hand's 3D viewmodel reuses the engine rasterizer's camera-space
 * overlay: the same vertex and material types, overlay depth, no world fog. */
#ifndef GEOMETRIUM_RENDER_INTERNAL_H
#define GEOMETRIUM_RENDER_INTERNAL_H
#include "geometrium_internal.h"
#include "engine/render/rend3d_internal.h"
#endif
