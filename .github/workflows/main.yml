name: Build Enjoer cube APK

on: [push, workflow_dispatch]

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: android-actions/setup-android@v3
        with:
          packages: "platforms;android-34 build-tools;34.0.0"

      - uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25c

      # Vulkan comes with the NDK, so the CI APK is the real GPU build.
      - name: Build native library
        run: |
          set -eu
          cmake -B build-arm64 \
            -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_ROOT/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-29 \
            -DANDROID_NDK=$ANDROID_NDK_ROOT \
            -DCMAKE_BUILD_TYPE=Release \
            -DENJOER_USE_VULKAN=ON
          cmake --build build-arm64 -j
          mkdir -p staging/lib/arm64-v8a
          cp build-arm64/libds_game.so staging/lib/arm64-v8a/

      - name: Compile Java activity
        run: |
          set -eu
          mkdir -p build-java staging
          javac -source 8 -target 8 \
            -classpath $ANDROID_HOME/platforms/android-34/android.jar \
            -d build-java \
            game/java/com/cb4/GameActivity.java
          $ANDROID_HOME/build-tools/34.0.0/d8 \
            --lib $ANDROID_HOME/platforms/android-34/android.jar \
            --output staging \
            $(find build-java -name '*.class')

      - name: Package APK
        run: |
          set -eu
          BT=$ANDROID_HOME/build-tools/34.0.0
          $BT/aapt package -f \
            -M game/AndroidManifest.xml \
            -I $ANDROID_HOME/platforms/android-34/android.jar \
            -F unsigned.apk \
            staging/
          $BT/zipalign -v 4 unsigned.apk aligned.apk
          keytool -genkey -v \
            -keystore d.ks \
            -storepass android \
            -alias a \
            -keypass android \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -dname "CN=Enjoer"
          $BT/apksigner sign \
            --ks d.ks \
            --ks-pass pass:android \
            --key-pass android \
            --out EnjoerCube.apk \
            aligned.apk

      - uses: actions/upload-artifact@v4
        with:
          name: Enjoer-Cube-APK
          path: EnjoerCube.apk
